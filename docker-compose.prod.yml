services:
  backend:
    image: ${ECR_REGISTRY}/slimkit/backend:latest
    container_name: slimkit-backend
    ports:
      - "3001:3001"
    environment:
      - ANTHROPIC_API_KEY=not-needed
    restart: unless-stopped

  frontend:
    image: ${ECR_REGISTRY}/slimkit/frontend:latest
    container_name: slimkit-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
